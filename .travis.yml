language: node_js
node_js:
 - "12"

branches:
  only:
    - master

notifactions:
  email: false

before_script :
 - curl -o- -L https://yarnpkg.com/install.sh | bash -s
 - export PATH=$HOME/.yarn/bin:$PATH
 - yarn install

after_success:
  - npx semantic-release
